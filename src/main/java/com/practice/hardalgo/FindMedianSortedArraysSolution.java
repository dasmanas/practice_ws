package com.practice.hardalgo;

public class FindMedianSortedArraysSolution {

    public double findMedianSortedArrays(int[] x, int[] y) {

        int xLen = x.length;
        int yLen = y.length;

        int xSplit = xLen / 2;
        int ySplit = ((xLen + yLen + 1) / 2) - xSplit;

//        if(x[xSplit-1])


        return 0;
    }
}
